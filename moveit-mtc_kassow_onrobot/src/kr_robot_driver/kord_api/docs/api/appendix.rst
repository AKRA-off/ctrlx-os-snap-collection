.. _appendix:

Appendix
========

.. |br| raw:: html

   <br />
   
In the following you can find additional details regarding the KORD api.

Alarm States Encoding
---------------------

The `systemAlarmState()` method provides the condensed state of the present error state of the RC. In case the robot controller is suspended due the ESTOP, PSTOP 
or any other kind of error, the function returns non-zero value that encodes the necessary information about this recent state. It consists of following:

- Category (Safety Event, Soft Stop, Hardware Status). Indicates the content of the condition identifier.
- Context represents a brief snapshot of other collateral alarm states present in the system. E.g. when the system was suspended due to some PSTOP,
  but as a consequence ended up in the ESTOP, both ESTOP|PSTOP flags should be contained.
- Condition, keeps the specific error identification.

In the following tables you find the useful condition identifier based on category.

Glossary
~~~~~~~~

**Trajectory reference** - references generated by the planner. Depending on the movement type for either joints or TCP.

**Robot model** -based on planner references calculates joint references such that the workspace trajectory is met and the robot remains within working envelope.

**Sensor** - state data measured by the joints, including position, velocity, torque.

Safety Event Conditions
~~~~~~~~~~~~~~~~~~~~~~~

.. list-table:: Table 1: Safety Event Conditions
   :widths: 30 10 50
   :header-rows: 1

   * - SAFETY EVENT Condition ID
     - Value
     - Description
   * - EXTERNAL_ESTOP_ACTIVATED
     - 1001
     - EStop was activated by either the EStop  |br| button or by a device.
   * - IOB_NOT_RESPONDING
     - 1002
     - IOBoard is not responding in a row of 5 updates.
   * - JBS_NOT_RESPONDING
     - 1003
     - JBoards are not responding in a row of 5 updates.
   * - JREF_X_SENSOR_POSITION_SPAN
     - 1004
     - The model joint position and sensor joint position |br| have deviated over a given threshold during |br| movement.
   * - JREF_POSITION_DELTA_SPAN
     - 1005
     - The difference between two consecutive joint |br|  positions in the model has exceeded acceptable |br| limit. 
   * - JRATED_SPEED_EXCEEDED
     - 1006
     - Joint model speed has exceeded rated joint |br| speed limit and appropriate safety |br| action will be taken.
   * - JRATED_TORQUE_EXCEEDED
     - 1007
     - Joint model torque has exceeded rated joint |br| torque limit and appropriate safety |br| action will be taken.
   * - JHOLD_TORQUE_EXCEEDED
     - 1008
     - Calculated static joint torque has exceeded static |br| joint torque limit. Static torque |br| is torque when the robot is holding |br| still in place.
   * - JBRATED_TEMP_EXCEEDED
     - 1009
     - Joint board rated temperature was exceeded.
   * - JTORQUE_DEVIATION_EXCEEDED
     - 1010
     - Joint expected and measured torques have deviated |br| over acceptable threshold.
   * - MODEL_X_TRJ_REFJ_SSPAN_EXC
     - 1011
     - The difference between trajectory reference joint |br| position and model joint position |br| has exceeded the given span limit. |br| PStop action will be taken.
   * - MODEL_X_TRJ_REFW_SSPAN_EXC
     - 1012
     - The difference between the norm of the trajectory |br| reference TCP position and the norm |br| of the model TCP position has |br| exceeded the given span limit. |br| Translations and rotations are considered |br| separately.
   * - FRAME_SPEED_LIMIT_EXC
     - 1013
     - Translation speed limit is exceeded by at least |br| one of the robot frames.
   * - EXTERNAL_PSTOP_ACTIVATED
     - 2001
     - PStop signal was activated by the IOBoard.


Soft Stop Conditions
~~~~~~~~~~~~~~~~~~~~

.. list-table:: Table 2: Soft Stop Conditions
   :widths: 30 10 50
   :header-rows: 1

   * - SOFT STOP Condition ID
     - Value
     - Description
   * - MODEL_INVALID_STATE
     - 2001
     - The robot model holds invalid values for joints |br|  or frames of the robot. Invalid values |br| considered are either not a number |br| or infinities.
   * - MODEL_JVELOCITY_LIMITS_EXC
     - 2002
     - Soft stop initiated after the model joints speed |br| exceeded internal limits.
   * - MODEL_JTORQUE_LIMITS_EXC
     - 2003
     - The model torque has exceeded the current |br| torque limits. The torque limits |br| for this case take into account the joint speed |br| and adjust the limit for the current speed.
   * - MODEL_JSDTORQUE_LIMITS_EXC
     - 2004
     - The generat torque has changed between two |br| updates and the new torque value |br| is exceeding the static torque limits. This event |br| is reported for example when load is changed.
   * - MODEL_JPOS_LIMITS_VIOLATION_EST
     - 2005
     - Preventive soft stop triggered in case joints |br| approach the limit based on |br| extrapolated estimation.
   * - CBUN_KORD_BAD_CONN_QUALITY      
     - 3001
     - CBun QoC (Quality of Connection monitor) |br| has detected a violation of the |br| connection parameters set |br| in the KORD.ini file. This error needs |br| to be cleared in order |br| to continue using the CBun.


KORD Event Conditions
~~~~~~~~~~~~~~~~~~~~~

.. list-table:: Table 3: KORD Event Conditions
   :widths: 30 10 50
   :header-rows: 1

   * - KORD Event Condition ID
     - Value
     - Description
   * - INFEASIBLE_MOVE_COMMAND
     - 3002
     - The controller has detected that the movement is not feasible |br| and it will be stopped.
   * - CBUN_KORD_COMM_ERROR
     - 3003
     - Communication error between the API and CBun.


HW Stat Conditions
~~~~~~~~~~~~~~~~~~

.. list-table:: Table 3: HW Stat Conditions
   :widths: 30 10 50
   :header-rows: 1

   * - HW STAT flags
     - Value
     - Description
   * - HW_STAT_LOW_VOLTAGE_AFTER_RELAY_DETECTED
     - 0x01
     - Reports low voltage, |br| indicating power should |br| be supplied to |br| the arm but is not. |br| This suggests a hardware |br| problem with the IO Board. 
   * - HW_STAT_INIT_BLOCKED
     - 0x02
     - Set when the initialization |br| process is unable to proceed, |br| requiring user intervention. |br| Causes may include |br| a hardware issue, such as |br| a loose cable, or |br| an inconsistent hardware |br| state, indicated by error |br| or status bits.
   * - HW_STAT_INIT_RUID_MISMATCH
     - 0x04
     - Stops initialization |br| due to an RUID mismatch, |br| meaning the controller |br| has identified an arm that |br| does not match the currently |br| used one. This could |br| occur when a new arm |br| is connected or when the arm |br| model is changed in |br| the settings. 
   * - HW_STAT_HARD_FAULT
     - 0x08
     - Signals that the IO Board |br| requires a power cycle |br| to allow for re-initialization. |br| The cause for this flag is |br| outside the controller scope.
   * - HW_STATUS_DISCOVERY_FAILED
     - 0x0100
     - Some of the devices were |br| not discovered on the |br| communication line indicating |br| a possible HW related error. |br| Possibly loose wire |br| or a faulty device.
   * - HW_STATUS_ERROR_BITS_SET
     - 0x0200
     - One or more devices have |br| present error bits |br| in their status. |br| The initialization will |br| fail as a result. |br| The bits need to be |br| retrieved and provided |br| to a support team |br| in order to analyze |br| the problem.
   * - HW_STATUS_DEVICE_DISABLED
     - 0x0400 
     - This flag is reported |br| when a HW device |br| is supposed to be replaced |br| by a software loopback.
   * - HW_STATUS_LOW_VOLTAGE
     - 0x0800
     - One or more devices |br| are underpowered - PSU |br| fails to deliver |br| expected voltage levels. |br| Suggests a problem |br| with a power supply |br| or a device itself.

